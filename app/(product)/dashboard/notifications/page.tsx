import PlaceholderPage from "@/features/app-shell/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      area="dashboard"
      eyebrow="Your account"
      title="Notifications"
      copy="See account-level updates and activity collected from every institution and private workspace you belong to."
    />
  );
}
