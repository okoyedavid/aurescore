import Link from "next/link";
import { ArrowRight, Building2, Plus, UserRoundCog } from "lucide-react";
import { Button } from "@/components/ui/Button";
import { workspaces } from "../data";

export default function WorkspaceOverview() {
  return <section><div className="flex items-center justify-between gap-4"><div><h2 className="font-display text-xl font-semibold">Your workspaces</h2><p className="mt-1 text-sm text-[var(--app-muted)]">Enter an institution or private score workspace.</p></div><Button type="button" variant="outline" className="h-10 min-h-10 px-4"><Plus size={16}/> <span className="hidden sm:inline">New workspace</span></Button></div><div className="mt-5 grid gap-4 md:grid-cols-2">{workspaces.map((workspace)=><Link key={workspace.name} href={workspace.href} className="app-panel group rounded-lg border border-[var(--app-border)] p-5 transition-colors hover:border-blue-500/60"><div className="flex items-start gap-4"><div className={`flex h-11 w-11 shrink-0 items-center justify-center rounded-md text-white ${workspace.accent}`}>{workspace.href==="/institution"?<Building2 size={20}/>:<UserRoundCog size={20}/>}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><h3 className="font-display text-lg font-semibold">{workspace.name}</h3><ArrowRight size={17} className="mt-1 shrink-0 text-[var(--app-muted)] transition-transform group-hover:translate-x-1"/></div><p className="mt-1 text-xs text-[var(--app-muted)]">{workspace.institution}</p><div className="mt-5 flex items-center justify-between border-t border-[var(--app-border)] pt-4"><span className="text-xs font-medium text-[var(--app-muted)]">{workspace.role}</span><span className="inline-flex items-center gap-1.5 text-[10px] font-semibold text-emerald-500"><span className="h-1.5 w-1.5 rounded-full bg-emerald-500"/>{workspace.status}</span></div></div></div></Link>)}</div></section>;
}
